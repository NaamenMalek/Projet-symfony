{# templates/author/add.html.twig #}

<h1>Ajouter un Auteur</h1>

{{ form_start(form) }}  {# Démarre le formulaire #}
    {{ form_widget(form) }}  {# Affiche tous les champs du formulaire #}
    <button type="submit" class="btn btn-primary">Ajouter Auteur</button> {# Bouton de soumission #}
{{ form_end(form) }}  

{# Termine le formulaire #}
